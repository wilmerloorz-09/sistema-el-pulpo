/** Ancho util en caracteres (fuente A, 80mm, ~203dpi). */
export const THERMAL_LINE_CHARS = 48;

/** Ancho en puntos para drivers que usan modo imagen (72mm area util). */
export const THERMAL_PRINT_WIDTH_DOTS = 576;

/** URL del puente local (Node) que envia bytes RAW a la impresora. */
export const DEFAULT_THERMAL_PRINT_BRIDGE_URL = "http://127.0.0.1:17888/print";
